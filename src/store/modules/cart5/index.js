const state = () => ({
  items: [],
  checkoutStatus: null
});
const getters = {};
const actions = {
  checkout() {}
};

const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};
